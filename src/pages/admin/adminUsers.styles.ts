import type { CSSProperties } from 'react'

export const adminUsersPageStyles: Record<string, CSSProperties> = {
  main: { flex: 1, overflowY: 'auto', background: 'var(--bg)', minHeight: '100vh' },
  wrapper: { padding: 24, maxWidth: 900, margin: '0 auto' },
  headerRow: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    gap: 16,
    flexWrap: 'wrap',
    marginBottom: 24,
  },
  title: { fontFamily: 'Syne, sans-serif', fontSize: 24, fontWeight: 800, color: 'var(--text)', marginBottom: 4 },
  subtitle: { color: 'var(--muted)', fontSize: 13, marginBottom: 0 },
  btnCreate: {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    background: 'linear-gradient(135deg, var(--accent-cyan), #00c4ad)',
    border: 'none',
    borderRadius: 12,
    padding: '10px 20px',
    fontSize: 13,
    fontWeight: 700,
    color: '#08130f',
    cursor: 'pointer',
    whiteSpace: 'nowrap',
  },
  error: {
    background: 'rgba(239,68,68,.15)',
    border: '1px solid #ef4444',
    borderRadius: 12,
    padding: '12px 16px',
    color: '#fca5a5',
    fontSize: 13,
    marginBottom: 24,
  },
  statsGrid: { display: 'grid', gridTemplateColumns: 'repeat(3, minmax(0, 1fr))', gap: 12, marginBottom: 24 },
  statCard: { background: 'var(--surface2)', border: '1px solid var(--border-bright)', borderRadius: 16, padding: 16 },
  statValue: { fontFamily: 'Syne, sans-serif', fontSize: 28, fontWeight: 900, color: 'var(--text)', margin: 0, lineHeight: 1.1 },
  card: { background: 'var(--surface)', border: '1px solid var(--border-bright)', borderRadius: 20, padding: 0, overflow: 'hidden' },
  table: { borderCollapse: 'collapse', width: '100%' },
  th: {
    textAlign: 'left',
    padding: '10px 20px',
    fontSize: 11,
    color: 'var(--table-header-color)',
    fontWeight: 600,
    textTransform: 'uppercase',
    letterSpacing: '0.05em',
    borderBottom: '1px solid var(--surface2)',
  },
  tr: {},
  td: { padding: '12px 20px', borderBottom: '1px solid var(--surface2)', fontSize: 13 },
  userCell: { display: 'flex', alignItems: 'center', gap: 10, minWidth: 0 },
  userName: { color: 'var(--text)', fontWeight: 500 },
  userEmail: { color: 'var(--muted)', fontSize: 11 },
  contact: { color: 'var(--muted)', fontSize: 12 },
  unit: { color: 'var(--muted)', fontSize: 12 },
  linkBtn: {
    background: 'none',
    border: 'none',
    color: 'var(--link-accent)',
    fontWeight: 600,
    fontSize: 12,
    cursor: 'pointer',
    textDecoration: 'underline',
    textUnderlineOffset: 2,
  },
  empty: { padding: 40, textAlign: 'center', color: 'var(--muted)', fontSize: 13 },
}

export const editUserModalStyles: Record<string, CSSProperties> = {
  root: {
    display: 'flex',
    overflow: 'hidden',
    borderRadius: 16,
    margin: -24,
  },
  accent: {
    width: 5,
    flexShrink: 0,
    background: 'linear-gradient(180deg, var(--accent-cyan), var(--accent-violet))',
  },
  body: {
    flex: 1,
    padding: '22px 20px 18px',
  },
  title: {
    fontSize: 11,
    fontWeight: 700,
    color: 'var(--accent-cyan)',
    textTransform: 'uppercase',
    letterSpacing: '0.09em',
    margin: '0 0 16px',
  },
  userRow: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    marginBottom: 20,
    paddingBottom: 16,
    borderBottom: '0.5px solid var(--surface2)',
  },
  userName: {
    color: 'var(--text)',
    fontSize: 14,
    fontWeight: 600,
    margin: '0 0 2px',
  },
  userEmail: {
    color: 'var(--muted)',
    fontSize: 11,
    margin: '0 0 1px',
  },
  userMeta: {
    color: 'var(--muted)',
    fontSize: 11,
    margin: 0,
  },
  sectionLabel: {
    fontSize: 10,
    color: 'var(--muted)',
    textTransform: 'uppercase',
    letterSpacing: '0.07em',
    margin: '0 0 9px',
  },
  editHint: {
    fontSize: 11,
    color: 'var(--muted)',
    margin: '0 0 10px',
    lineHeight: 1.45,
  },
  pillGroup: {
    display: 'flex',
    gap: 7,
    flexWrap: 'wrap',
    marginBottom: 18,
  },
  pill: {
    padding: '7px 15px',
    borderRadius: 99,
    border: '1px solid var(--surface2)',
    fontSize: 12,
    color: 'var(--muted)',
    background: 'transparent',
    cursor: 'pointer',
    transition: 'all .15s',
  },
  footer: {
    display: 'flex',
    justifyContent: 'flex-end',
    gap: 10,
    marginTop: 8,
    paddingTop: 16,
    borderTop: '0.5px solid var(--surface2)',
  },
  btnCancel: {
    background: 'transparent',
    border: '1px solid var(--surface2)',
    borderRadius: 10,
    padding: '9px 18px',
    fontSize: 13,
    color: 'var(--muted)',
    cursor: 'pointer',
  },
  btnSave: {
    background: 'var(--accent-cyan)',
    border: 'none',
    borderRadius: 10,
    padding: '9px 22px',
    fontSize: 13,
    fontWeight: 700,
    color: '#08130f',
    cursor: 'pointer',
    transition: 'background .2s',
  },
  field: { display: 'flex', flexDirection: 'column', gap: 5 },
  select: {
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    borderRadius: 10,
    padding: '9px 12px',
    fontSize: 13,
    color: 'var(--text)',
    outline: 'none',
    width: '100%',
    boxSizing: 'border-box',
    cursor: 'pointer',
  },
  fieldHint: {
    fontSize: 10,
    color: 'var(--muted)',
    margin: '4px 0 0',
    lineHeight: 1.4,
  },
  inlineError: {
    background: 'rgba(248,113,113,.12)',
    border: '1px solid rgba(248,113,113,.4)',
    borderRadius: 10,
    padding: '10px 12px',
    color: '#fca5a5',
    fontSize: 12,
    marginBottom: 14,
  },
}

export const createUserModalStyles: Record<string, CSSProperties> = {
  root: { display: 'flex', overflow: 'hidden', borderRadius: 16, margin: -24 },
  accent: { width: 5, flexShrink: 0, background: 'linear-gradient(180deg, var(--accent-cyan), var(--accent-violet))' },
  body: { flex: 1, padding: '22px 20px 18px' },
  modalHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
    marginBottom: 16,
  },
  title: {
    fontSize: 11,
    fontWeight: 700,
    color: 'var(--accent-cyan)',
    textTransform: 'uppercase',
    letterSpacing: '.09em',
    margin: 0,
  },
  inlineError: {
    background: 'rgba(248,113,113,.12)',
    border: '1px solid rgba(248,113,113,.4)',
    borderRadius: 10,
    padding: '10px 12px',
    color: '#fca5a5',
    fontSize: 12,
    marginBottom: 14,
  },
  steps: { display: 'flex', alignItems: 'center', gap: 6, marginBottom: 20 },
  dot: {
    width: 26,
    height: 26,
    borderRadius: '50%',
    border: '1px solid var(--border-bright)',
    fontSize: 11,
    fontWeight: 700,
    color: 'var(--muted)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    background: 'transparent',
    flexShrink: 0,
  },
  stepLine: { flex: 1, height: 1, background: 'var(--surface2)', transition: 'background .3s' },
  row2: { display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 12 },
  field: { display: 'flex', flexDirection: 'column', gap: 5 },
  fieldLabel: { fontSize: 10, color: 'var(--muted)', textTransform: 'uppercase', letterSpacing: '.07em' },
  input: {
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    borderRadius: 10,
    padding: '9px 12px',
    fontSize: 13,
    color: 'var(--text)',
    outline: 'none',
    width: '100%',
    boxSizing: 'border-box',
  },
  select: {
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    borderRadius: 10,
    padding: '9px 12px',
    fontSize: 13,
    color: 'var(--text)',
    outline: 'none',
    width: '100%',
    boxSizing: 'border-box',
    cursor: 'pointer',
  },
  fieldHint: {
    fontSize: 10,
    color: 'var(--muted)',
    margin: '4px 0 0',
    lineHeight: 1.4,
  },
  sectionLabel: {
    fontSize: 10,
    color: 'var(--muted)',
    textTransform: 'uppercase',
    letterSpacing: '.07em',
    margin: '0 0 9px',
  },
  pillGroup: { display: 'flex', gap: 7, flexWrap: 'wrap', marginBottom: 18 },
  pill: {
    padding: '7px 15px',
    borderRadius: 99,
    border: '1px solid var(--surface2)',
    fontSize: 12,
    color: 'var(--muted)',
    background: 'transparent',
    cursor: 'pointer',
  },
  toggleRow: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
    marginBottom: 4,
  },
  toggleLabel: { fontSize: 12, color: 'var(--muted)', marginBottom: 2 },
  toggleSub: { fontSize: 10, color: 'var(--muted)', lineHeight: 1.45 },
  toggle: {
    width: 36,
    height: 20,
    borderRadius: 99,
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    position: 'relative',
    cursor: 'pointer',
    flexShrink: 0,
    transition: 'background .15s',
    padding: 0,
  },
  toggleKnob: {
    width: 14,
    height: 14,
    borderRadius: '50%',
    background: 'var(--muted)',
    position: 'absolute',
    top: 2,
    left: 2,
    transition: 'all .2s',
    pointerEvents: 'none',
  },
  previewChip: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    borderRadius: 12,
    padding: '12px 14px',
    marginBottom: 14,
  },
  previewAvatar: {
    width: 40,
    height: 40,
    borderRadius: '50%',
    background: 'rgba(0, 229, 200, 0.15)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: 14,
    fontWeight: 700,
    color: 'var(--accent-cyan)',
    flexShrink: 0,
  },
  previewName: { color: 'var(--text)', fontSize: 14, fontWeight: 600, marginBottom: 2 },
  previewMeta: { color: 'var(--muted)', fontSize: 11 },
  summary: {
    background: 'var(--surface2)',
    border: '1px solid var(--border-bright)',
    borderRadius: 12,
    padding: 14,
    fontSize: 12,
    lineHeight: 2,
  },
  summaryRow: { display: 'flex', justifyContent: 'space-between' },
  summaryKey: { color: 'var(--muted)' },
  summaryVal: { color: 'var(--text)' },
  footer: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 10,
    marginTop: 8,
    paddingTop: 14,
    borderTop: '.5px solid var(--surface2)',
  },
  stepHint: { fontSize: 11, color: 'var(--muted)' },
  btnCancel: {
    background: 'transparent',
    border: '1px solid var(--surface2)',
    borderRadius: 10,
    padding: '9px 16px',
    fontSize: 13,
    color: 'var(--muted)',
    cursor: 'pointer',
  },
  btnSave: {
    background: 'var(--accent-cyan)',
    border: 'none',
    borderRadius: 10,
    padding: '9px 22px',
    fontSize: 13,
    fontWeight: 700,
    color: '#08130f',
    cursor: 'pointer',
    transition: 'background .2s',
  },
}
